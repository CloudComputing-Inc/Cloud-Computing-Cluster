apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
data:
  nginx.conf: |
    events {}

    http {
        upstream api_gateway {
            server api-gateway-svc:80;
        }

        upstream yelp_reviews {
            server yelp-reviews-svc:80;
        }

        server {
            listen 80;

            location /api/ {
                proxy_pass http://api_gateway/;
            }

            location /yelp/ {
                proxy_pass http://yelp_reviews/;
            }
        }
    }
